# Mosquitto — dev config (used by docker-compose_LOCAL.yml from ryan/pqc-transport)
# WebSocket on 9001 so the PQC reverse proxy can forward MQTT-over-WS traffic

listener 9001 0.0.0.0
protocol websockets
allow_anonymous true
