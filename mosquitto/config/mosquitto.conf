# Mosquitto MQTT broker — local dev configuration.
# Anonymous access is OK because the broker only listens on the internal
# Docker network and the sole client is Node-RED in the same compose project.

listener 1883 0.0.0.0
allow_anonymous true
persistence false

log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
